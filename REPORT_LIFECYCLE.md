# 🔄 Жизненный цикл заявки (Report) в TazaQala

## 📊 Статусы заявки

### Основные статусы (`status`):
- **`pending`** — на проверке (ожидает модерации)
- **`confirmed`** — подтверждено (готово к уборке)
- **`pending_verification`** — ожидает проверки уборки (клинер завершил, админ проверяет)
- **`cleaned`** — убрано (финальный статус)
- **`rejected`** — отклонено

### AI статусы (`ai_status`):
- **`auto_confirmed`** — автоматически подтверждено AI (confidence ≥ 0.85)
- **`needs_review`** — требует проверки модератором (0.50 ≤ confidence < 0.85)
- **`rejected`** — отклонено AI (confidence < 0.50)

---

## 🔄 Полный цикл заявки

### 1️⃣ **Создание заявки** (`/reports/new`)

**Действия:**
- Пользователь загружает фото, указывает координаты, описание
- Система сохраняет файл в `static/uploads/`
- **AI-модерация** анализирует фото через OpenAI Vision API

**AI принимает решение:**
```
confidence ≥ 0.85 + trash_detected = True  →  auto_confirmed
0.50 ≤ confidence < 0.85                   →  needs_review
confidence < 0.50                          →  rejected
```

**Результат:**
- Если `ai_status = 'auto_confirmed'` → `status = 'confirmed'` (сразу подтверждено)
- Если `ai_status = 'needs_review'` → `status = 'pending'` (на проверку модератору)
- Если `ai_status = 'rejected'` → `status = 'pending'` (но с низким приоритетом)

**Начисление баллов:**
- Автору: `POINTS_CONFIRMED_REPORT` (обычно 10)
- + `POINTS_WITH_GPS_COMMENT` если есть описание (обычно 5)
- Если сразу `confirmed` → `confirmed_reports += 1`

**Уведомление:**
- Автору: "Ваш репорт получен! Статус: {status}. Начислено {points} баллов."

---

### 2️⃣ **Модерация** (`/admin/report/<id>/moderate`)

**Кто:** Администратор/модератор

**Действия:**
- Просматривает репорты с `ai_status = 'needs_review'` в админ-панели
- Может **одобрить** или **отклонить**

**При одобрении (`approve`):**
```python
status = 'confirmed'
ai_status = 'auto_confirmed'  # Чтобы исчез из списка "на проверке"
moderator_id = current_user.id
moderated_at = datetime.utcnow()
```
- Автору начисляются баллы: `POINTS_CONFIRMED_REPORT` + бонус за описание
- `author.confirmed_reports += 1`
- Уведомление: "Ваш репорт #{id} подтвержден модератором! +{points} баллов."

**При отклонении (`reject`):**
```python
status = 'rejected'
ai_status = 'rejected'
moderator_id = current_user.id
moderated_at = datetime.utcnow()
```
- Уведомление автору (если есть)

---

### 3️⃣ **Уборка** (два пути)

#### 🛣️ **Путь A: Через клинера** (`/cleaner/report/<id>/complete`)

**Кто:** Пользователь с `is_cleaner = True` или `role = 'admin'`

**Действия:**
1. Клинер видит список `status = 'confirmed'` в панели `/cleaner/`
2. Выбирает репорт для уборки
3. Загружает:
   - Фото **после** уборки (`after_photo`)
   - Документ об утилизации (`doc_photo`)
4. Система сохраняет в `static/uploads/cleanup/`

**Обновление репорта:**
```python
status = 'pending_verification'  # ⚠️ Новый статус!
cleaned_at = datetime.utcnow()
cleaned_by_id = current_user.id
cleaned_photo_path = 'cleanup/after_xxx.jpg'
disposal_document_path = 'cleanup/doc_xxx.jpg'
```

**Уведомления:**
- Всем админам/модераторам: "Требуется проверка уборки репорта #{id}"

---

#### 🛣️ **Путь B: Волонтёрская уборка** (`/reports/<id>/mark_cleaned`)

**Кто:** Любой авторизованный пользователь

**Действия:**
1. Пользователь видит репорт на карте/детальной странице
2. Загружает фото **после** уборки
3. Система сразу меняет статус

**Обновление репорта:**
```python
status = 'cleaned'  # ⚠️ Сразу финальный статус!
cleaned_at = datetime.utcnow()
cleaned_by_id = current_user.id
cleaned_photo_path = 'cleaned_xxx.jpg'
```

**Начисление баллов:**
- Автору репорта: `POINTS_CLEANED_REPORT` (обычно 50)
- Волонтёру: `POINTS_CLEANED_REPORT` (обычно 50)

**Уведомления:**
- Автору: "Отличная новость! Ваш репорт #{id} убран. +50 баллов!"

---

### 4️⃣ **Верификация уборки** (`/admin/verify-cleanup/<id>`)

**Кто:** Администратор (только для репортов со статусом `pending_verification`)

**Действия:**
1. Админ видит список `status = 'pending_verification'` в дашборде
2. Просматривает фото "до" и "после", документ об утилизации
3. Может **одобрить** или **отклонить**

**При одобрении (`approve_cleanup`):**
```python
status = 'cleaned'  # Финальный статус
moderator_id = current_user.id
moderated_at = datetime.utcnow()
```

**Начисление баллов:**
- Клинеру: +20 баллов
- Автору репорта: `POINTS_CLEANED_REPORT` (обычно 50)

**Уведомления:**
- Клинеру: "Ваша уборка репорта #{id} подтверждена! +20 баллов."
- Автору: "Ваш репорт #{id} убран! Локация очищена."

**При отклонении (`reject_cleanup`):**
```python
status = 'confirmed'  # Возвращаем обратно
```
- Репорт снова доступен для уборки

---

## 📈 Диаграмма состояний

```
┌─────────────┐
│   Создание   │
│   (new)      │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────┐
│      AI-модерация                │
│  (OpenAI Vision API)             │
└──────┬───────────────────────────┘
       │
       ├─ confidence ≥ 0.85 ────────┐
       │                             │
       ├─ 0.50 ≤ conf < 0.85 ────────┤
       │                             │
       └─ conf < 0.50 ───────────────┤
                                     │
       ┌─────────────────────────────▼──────────────┐
       │                                             │
       ▼                                             ▼
┌──────────────┐                          ┌──────────────┐
│  confirmed   │                          │   pending    │
│ (auto)       │                          │ (needs_review)│
└──────┬───────┘                          └──────┬───────┘
       │                                          │
       │                                          │ Модератор
       │                                          ▼
       │                                  ┌──────────────┐
       │                                  │  confirmed   │
       │                                  │  (manual)    │
       │                                  └──────┬───────┘
       │                                         │
       └─────────────────────────────────────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │   Уборка              │
            │                       │
            ├─ Клинер ──────────────┤
            │  (2 фото)             │
            │                       │
            └─ Волонтёр ────────────┘
                       │
            ┌──────────┴──────────┐
            │                     │
            ▼                     ▼
    ┌──────────────┐      ┌──────────────┐
    │pending_      │      │   cleaned    │
    │verification │      │  (volunteer)  │
    └──────┬───────┘      └──────────────┘
           │
           │ Админ проверяет
           ▼
    ┌──────────────┐
    │   cleaned    │ ◄─── ФИНАЛЬНЫЙ СТАТУС
    └──────────────┘
```

---

## 🎯 Ключевые моменты

1. **AI-модерация** происходит сразу при создании
2. **Два пути уборки:**
   - Через клинера → требует верификации админом
   - Волонтёрская → сразу `cleaned`
3. **Статус `pending_verification`** — только для клинеров, требует проверки
4. **Баллы начисляются:**
   - При создании (если сразу `confirmed`)
   - При модерации (если одобрено)
   - При уборке (автору и уборщику)
   - При верификации (клинеру +20)
5. **Уведомления** отправляются на каждом этапе

---

## 🔍 Где смотреть статусы

- **Админ-панель** (`/admin/dashboard`):
  - Репорты на проверке: `ai_status = 'needs_review'`
  - Уборки на проверке: `status = 'pending_verification'`
  
- **Карта** (`/map`):
  - Показывает: `confirmed`, `cleaned`
  - Фильтры по статусам

- **API** (`/api/reports?status=confirmed`):
  - Можно фильтровать по любому статусу

